import { render, screen } from '@testing-library/react';

import { {{pascalCase name}} } from '.';

describe('<{{pascalCase name}}/>', () => {
  it('should be able render {{pascalCase name}} component correctly', () => {
    render(<{{pascalCase name}} />);
  });
});
